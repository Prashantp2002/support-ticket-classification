# --- Core app (API + classic ML) ---
fastapi~=0.115
uvicorn[standard]~=0.30
pydantic~=2.7
scikit-learn~=1.4
pandas~=2.2
numpy~=1.26
joblib~=1.3
prometheus_client~=0.20

# --- Azure Blob (if you store/load models or data from Azure Storage) ---
azure-storage-blob~=12.19
azure-identity~=1.16

# --- OPTIONAL: Only if your code actually uses Transformers/TensorFlow ---
# They are heavy; omit unless required by the repo code.
# tensorflow~=2.15
# transformers~=4.42
